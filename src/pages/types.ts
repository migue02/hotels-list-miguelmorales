import type { NativeStackScreenProps } from '@react-navigation/native-stack';

import { RootStackParamList } from '../navigation/types';

export type HotelsProps = NativeStackScreenProps<RootStackParamList, 'Hotels'>;
